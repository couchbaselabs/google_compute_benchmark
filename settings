PREFIX=$(whoami)
PROJECT="cb-googbench-101"
ZONE="us-central1-f"
NUM_SERVERS=8
NUM_CLIENTS=32
SERVER_SNAPSHOT="${PREFIX}-couchbase-server"
BASE_IMAGE_NAME="${PREFIX}-server-template"
CLIENT_SNAPSHOT="${PREFIX}-client-image"
CB_DATA_DISK="${PREFIX}-data-disk"
CB_DATA_SNAPSHOT="${PREFIX}-data-snapshot"
SERVER_TYPE="n1-standard-16"
CLIENT_TYPE="n1-standard-8"
DATA_DISK_TYPE="pd-ssd"  # "pd-standard" or "pd-ssd"
DATA_DISK_SIZE=500
USER=Administrator
PASS=password
INSTANCE_CREATE_BATCH_SIZE=4

NODE_PREFIX="${PREFIX}-server"
NODE_CLIENT_PREFIX="${PREFIX}-client"
